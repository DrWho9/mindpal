# MindPal public site (DayStart product)

Served at https://drwho9.github.io/mindpal/

This branch hosts a production build of DayStart (from private `DrWho9/mindpal-daystart`) with Vite `base: "/mindpal/"`.

- Demo locally: on Mac Mini, `npm run dev` in the DayStart project path
- Rebuild: on Mini `npm run build` then replace this repo root with `dist/` contents
- Thin phone shell archived on branch `thin-shell-archive`

Book / Videos phone modules lived in the prior shell; product home is now full DayStart (Verse WEB + 210 readings + Listen/Copy/Share + original DayStart UI).
