{
  "name": "mindpal-pages",
  "private": true,
  "type": "module",
  "description": "GitHub Pages shell for MindPal. Source of the live UI is the published DayStart build; this package patches Pack A + open-draft videos.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs"
  }
}
